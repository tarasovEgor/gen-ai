# Домашнее задание к Семинару 6

**Тема:** Планировщик-Исполнитель-Критик на прочность — валидатор схемы,
параллельность, замер угодливости

**Дедлайн:** 19.06.26 23:59

---

## Что нужно сделать

### 1. Добавить валидатор схемы между Планировщиком и Исполнителем

На семинаре мы увидели, что PWC не чинит галлюцинацию инструментов.
```python
VALID_TOOLS = {"get_fx_rate", "get_key_rate", "get_inflation", "calculate"}

def validate_plan(plan: Plan) -> list[str]:
    """Вернуть список ошибок плана (пустой — всё ок)."""
    # TODO
    return errors
```

Встройте вызов в `run_pwc()` сразу после `planner(...)`, если ошибки присутствуют, то
вызовите `planner(question, feedback=f"Инструменты не существуют: {errors}")`.

**Проверьте на Q3 из eval:**

```bash
python orchestrator.py "Какова накопленная инфляция с января 2022 по март 2026?"
```

После фикса Планировщик либо вернёт честный план «calculate по списку ипц
за 51 месяц» (который Исполнитель осилит), либо пустой план с объяснением
«такой задачи одним вызовом инструмента не решить» — и это правильное поведение.

> **Не нужно** переписывать сам agent_s5.

### 2. Параллельное исполнение независимых подвопросов

Сейчас Исполнители запускаются по одному, даже если `depends_on=[]` у всех
троих. Подвопросы одного уровня зависимостей можно звать через
`concurrent.futures.ThreadPoolExecutor`.

Сигнатура:

```python
from concurrent.futures import ThreadPoolExecutor

def execute_level(level: list[SubQuestion], prev_answers) -> dict[int, WorkerAnswer]:
    """Прогнать все подвопросы уровня параллельно."""
    # TODO
```

Модифицируйте `_topological_sort` → `_topological_levels`, чтобы возвращать
не плоский список, а список уровней (между уровнями есть зависимость,
внутри уровня — нет).

**Замерьте ускорение** на Q1 (рост курса USD): один прогон последовательно
против параллельного. Цифру — в отчёт.

### 3. Замерьте критику: temperature 0.0 против 0.7


Проверьте, работает ли Критик как зеркало Планировщика при нулевой температуре.

Подготовьте **5 заведомо битых** наборов ответов (прямо в коде):

```python
FAKE_BROKEN = [
    # арифметика без calculate:
    {"plan": Plan(reasoning="...", subquestions=[
        SubQuestion(id=1, question="курс USD?", expected_tools=["get_fx_rate"]),
        SubQuestion(id=2, question="курс EUR?", expected_tools=["get_fx_rate"]),
    ]),
     "answers": {1: WorkerAnswer(..., answer="USD=82.5, EUR=89, разница=6.5",
                                 used_tools=["get_fx_rate"])}},
    # выдуманное число:
    # несогласованные данные между подвопросами:
    # ...
]
```

Прогоните каждый набор:
- 10 раз с Критиком при `temperature=0.0`
- 10 раз с Критиком при `temperature=0.7`

Посчитайте, в скольких прогонах Критик ошибочно поставил `ok=True`
(ложное принятие). Запишите в таблицу:

| Битый кейс               | T=0.0, ложных принятий | T=0.7, ложных принятий |
|--------------------------|------------------------|------------------------|
| арифметика без calculate | X/10                   | Y/10                   |
| ...                      |                        |                        |

### 4. Расширьте eval до 6 вопросов

К Q1-Q3 добавь 3 своих. Требования:

- **1 вопрос** должен ГАРАНТИРОВАННО починиться добавлением валидатора
  (одиночный агент и PWC без валидатора падают, а PWC + валидатор справляется).
- **1 вопрос** с естественной параллельностью (3+ независимых подвопроса),
  на котором меряешь ускорение из части 2.
- **1 реальный** вопрос, лично тебе интересный по макроэкономике.

Прогоните **три** конфигурации:
  1. одиночный агент (С5)
  2. pwc без валидатора
  3. pwc + валидатор

Таблица в отчёте: id / query / одиночный N=5 / pwc N=5 / pwc+валидатор N=5.
Это ~90 запусков.

### 5. Короткий отчёт

Структура:

1. **Валидатор схемы** — сигнатура + 1-2 примера пойманных планов.
2. **Параллельность** — замеры по Q1 и бонусному вопросу.
3. **Замер критики** — таблица ложных принятий по T=0.0 и T=0.7. Одна
   фраза: работает ли гипотеза «шум лечит зеркальное соглашение».
4. **Eval 6×3** — таблица доли прохождения по трём конфигурациям.
5. **Вывод** — при каких условиях паттерн PWC оправдан, а когда
   лучше остаться с одиночным агентом.

---

## Критерии оценки

| Пункт                                                       | Баллы |
|-------------------------------------------------------------|------:|
| Валидатор схемы ловит минимум 2 типа выдуманных инструментов |     2 |
| Параллельное исполнение + измеренное ускорение              |     1 |
| Замер угодливости с честным N=10 на T=0 и T=0.7             |     2 |
| Eval на 6 вопросах × 3 конфигурации × N≥3                   |     2 |
| Отчёт: вывод про оправданность PWC                          |     1 |
| **Всего**                                                   |    8 |
